package params

var(
	// Boolean flag to indicate if we should create empty secrets.
	CreateSecrets bool = false

	// Boolean file to indicate we want to build from local directory.
	LocalBuild bool = false
)