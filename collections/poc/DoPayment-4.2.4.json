{
  "kind": "MetaGraf",
  "version": "v0alpha1",
  "metadata": {
    "name": "DoPayment",
    "version": "4.2.3",
    "description": "DoPayment performes monetary transactions such as withdrawal from and deposit to the external user account. In addition the service is verifying Responsible Gaming limits, and doing logging for later reconciliation.",
    "labels": {
    },
    "annotations": {
      "norsk-tipping.no/component_type": "internal",
      "norsk-tipping.no/exposable": "false",
      "norsk-tipping.no/appcode": "123",
      "norsk-tipping.no/tags": "betaling"
    }
  },
  "spec": {
    "resources": [
      {
        "name": "RGValidation",
        "type": "service",
        "version": "4.3.3",
        "match": ">=",
        "required": "true"
      }
    ],
    "environment": {
      "local": [
        {
          "name": "LocalVariableName",
          "required": "true",
          "type": "string",
          "description": "Some local variable name description"
        }
      ],
      "external": {
        "consumes": [
          {
            "name": "VariableName",
            "required": "true",
            "description": "Some external variable description"
          }
        ],
        "introduces": []
      }
    },
    "config": [
      {
        "filename": "string",
        "options": [
          {
            "name": "Configuration option",
            "required": "false",
            "description": "This configuration option does less than nothing",
            "type": "boolean",
            "default": "false"
          }
        ]
      }
    ]
  }
}